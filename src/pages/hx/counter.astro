---
export const partial = true;

import { turso } from '../../turso';
const counterResult = await turso.execute('SELECT counter FROM counters WHERE id = 1');
const count = counterResult.rows[0]["counter"];
---
{count}